# ML_ysc
机器学习记录

## 注意事项
1. 使用./.venv的虚拟环境(Python3.11.4),解释器选择该路径下的python.
2. 对于.ipynb文件,如果需要即使编译,需要在虚拟环境中更新内核,使用命令<pre><code>./.venv/bin/python -m pip install ipykernel -U --force-reinstall</pre></code>
